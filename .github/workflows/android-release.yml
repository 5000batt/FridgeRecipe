name: FridgeRecipe Production Release

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Create local.properties
        run: |
          echo "admob_app_id=${{ secrets.ADMOB_APP_ID }}" >> local.properties
          echo "admob_banner_id=${{ secrets.ADMOB_BANNER_ID }}" >> local.properties
          echo "admob_reward_id=${{ secrets.ADMOB_REWARD_ID }}" >> local.properties

      - name: Create google-services.json
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: echo "$GOOGLE_SERVICES_JSON" > app/google-services.json

      - name: Build Release AAB
        run: ./gradlew bundleRelease

      - name: Sign Release AAB
        uses: r0adkll/sign-android-release@v1
        id: sign_app
        with:
          releaseDirectory: app/build/outputs/bundle/release
          signingKeyBase64: ${{ secrets.KEYSTORE_BASE64 }}
          alias: ${{ secrets.KEY_ALIAS }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "34.0.0"

      - name: Create Release Notes
        run: |
          mkdir whatsnew
          echo "${{ github.event.head_commit.message }}" > whatsnew/whatsnew-ko-KR

      - name: Upload to Google Play
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_STORE_CONFIG_JSON }}
          packageName: com.kjw.fridgerecipe
          releaseFiles: ${{steps.sign_app.outputs.signedReleaseFile}}
          track: production
          whatsNewDirectory: whatsnew

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            ## FridgeRecipe 새 버전 출시
            
            **변경 사항:**
            ${{ github.event.head_commit.message }}
          files: ${{ steps.sign_app.outputs.signedReleaseFile }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Send Slack Notification (Success)
        if: success()
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_CD_WEBHOOK_URL }}
          SLACK_TITLE: "🚀 FridgeRecipe 프로덕션 배포 성공!"
          SLACK_MESSAGE: |
            *버전 코드:* ${{ env.VERSION_CODE }}
            *내용:* 구글 플레이 스토어 프로덕션에 새 버전이 업로드되었습니다.
            
            *커밋 메시지:* ${{ github.event.head_commit.message }}
          SLACK_COLOR: "#36a64f"
          SLACK_USERNAME: "Release Bot"
          SLACK_ICON_EMOJI: ":tada:"

      - name: Send Slack Notification (Failure)
        if: failure()
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_CD_WEBHOOK_URL }}
          SLACK_TITLE: "🚨 FridgeRecipe 프로덕션 배포 실패!"
          SLACK_MESSAGE: |
            *상태:* 구글 플레이 스토어 업로드 중 오류가 발생했습니다.
            워크플로우 로그를 확인하여 원인을 파악해 주세요.
            
            *커밋:* ${{ github.event.head_commit.message }}
            *실행 링크:* https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          SLACK_COLOR: "#ff0000"
          SLACK_USERNAME: "Release Bot"
          SLACK_ICON_EMOJI: ":warning:"